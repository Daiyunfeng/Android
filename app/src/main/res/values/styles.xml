<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme">
    </style>

    <!--splash主题-->
    <!-- 如果你的SplashActivity设置了layout文件，那么其在app完全初始化完成后才会显示，
    带来一定的耗时。这里直接以theme作为SplashActivity展示的UI，减少了加载时间达到秒开
    的效果。使用该启动画面实现方式亦可以避免有些app点开时出现的白屏问题。 -->
    <style name="Theme.Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@mipmap/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="BaseTheme" parent="Theme.AppCompat.Light.DarkActionBar"/>

    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="BaseNoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="OverflowMenuStyle"
           parent="@style/Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:colorBackground">@color/colorPrimary</item>
    </style>
</resources>
